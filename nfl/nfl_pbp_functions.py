import numpy as np
import pandas as pd
import os

# def add_play_duration(grouped_playbyplay_df):
#     result_df = grouped_playbyplay_df.iloc[::-1]  # reverse the dataframe
#     result_df["PlayDuration"] = 0
#     current_game_id = 0
#     last_play_duration = -1
#     for index, row in result_df.iterrows():
#         if(row["GameID"] == current_game_id):


